我是一个嵌入式软件设计工程师，有时候会遇到各种软件组件，这些组件之间有各种接口。大家如果了解 AutoSAR 的话，都知道会有各种各样的 SWC。

为了可视化地展示这些 SWC 之间的接口，我想做这样一个工具。这个工具的主要功能如下：

1. 互动展示：能够展示当前的 SWC 和其他 SWC 之间交互的接口。
2. 交互定位：点击 SWC 时，会把自身的 SWC 放在最左侧，目标 SWC 放在右侧。
3. 实时提示：同时会有连线提示以及 Port 相关的信息浮动展示。
